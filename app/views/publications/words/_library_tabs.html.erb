<div class="tabs">
  <%= link_to "Words", account_publication_words_path(publication_id: publication.id),
        class: ("active" if section == :words) %>
  <%= link_to "Sponsors", account_publication_sponsors_path(publication_id: publication.id),
        class: ("active" if section == :sponsors) %>
</div>
